# Storybook Component Surface

Real Storybook setup for IDP integration demos.

## Run

```bash
cd tools/wiki/apps/storybook
npm install
npm run dev
```

Default URL: `http://localhost:6006`.

Nuxt shell integration route: `/_storybook/**` and user-facing `/storybook`.
