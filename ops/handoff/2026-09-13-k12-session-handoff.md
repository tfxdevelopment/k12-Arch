# K12 session handoff — 2026-09-13 (cloud session → local Claude Desktop session)

Read this first, then `security/azure-remediation/plans/08-Gov-Preprod-Deploy-and-CICD-Plan.md` (the approved implementation plan), and continue at its **B3 step 1** (Terry's WSL discovery) / **step 2** (branch preparation).

Session of record: `https://claude.ai/code/session_01QETvmMy74iBEHm3QVfqpBt` (cloud). Terry's constraints and the delivery history below are the state as of 2026-09-13 ~04:00 UTC.

## Why this handoff exists

Terry asked (2026-09-12) for help (1) deploying the K12 infrastructure from Terraform, after first discussing how far to move it toward the "ideal" target state in the remediation package, (2) setting up, fixing and optimizing the ADO pipelines and agent pools into a scalable CI/CD system, and (3) capturing this session's context so a fresh local session (Claude Desktop, Windows/WSL) can continue without restarting. Answers given (2026-09-12/13): Gov tenant is now in scope; the first target is **Gov preprod, deployable** (prod later), stacked on Angelo's ADO PR 6078; the safe hardening bundle is in scope; discovery and plan/apply run **from Terry's WSL machine**; Gov agents run as **Container Apps Jobs**; ADO PR 6024 pipeline is **not** to be queued; nothing else on the K12-8804 items for now.


# Session context capture

## A1. Standing rules for this work

- Ask clarifying questions before detailed answers (Terry's preference).
- **No writes to Azure, Azure DevOps, or Atlassian without Terry's explicit approval.** Approved so far: opening ADO draft PR 6024 and amending it (commit 2 + description). Not approved: queueing pipelines, Jira comments, ACR deletes, Gov changes.
- **No AI attribution on anything pushed to ADO** (no "Generated by Claude Code" footer, no `Co-Authored-By: Claude`, no `Claude-Session:` trailers) in commits, PR text, or branch content. GitHub tfxdevelopment repos are Terry's own and may carry the normal attribution.
- The tfxdevelopment GitHub repos are private mirrors of the ADO repos and may be stale (k12-query-builder mirror is an April 2026 "GitOps baseline"; ADO `development` is the source of truth for app code).
- Gov tenant: previously "skip for now"; **as of 2026-09-12 in scope**.

## A2. Repos, branches, PRs

GitHub (tfxdevelopment): k12-infra (default development), devcontainer-platform (main), k12-Arch (development), k12-api-enrollment (development), .github-private (main), k12-query-builder (main). Session branch on all six: `claude/k12-devcontainer-azure-research-ky409a`; it exists on the remote only for devcontainer-platform, k12-Arch and .github-private.

Merged: k12-Arch #144 (remediation package, squash 2cfd3ed), k12-Arch **#148 (K12-8804 evidence pack, merged by Terry 2026-09-12 23:31 UTC, head bea49a8)**, devcontainer-platform #4, .github-private #4. Follow-up work in k12-Arch must start from a **fresh branch off `development`**, not the merged branch.

ADO: org `https://dev.azure.com/CFI-AzureDevOps`, project `K12`. Repo `k12-query-builder` id `f7a8859c-0ae4-4fba-bc37-885953aafa96`; `development` head `b54a2554` (Terry's PR 6056, UAT release-candidate pipeline, 2026-09-09); `main` is a stripped GitOps baseline. **Draft PR 6024** `fix/k12-8804-image-vulns` → development, commits `dcdb98be` + `4a80bb57`, 8 files, mergeable, no reviewers, no comments, no builds: https://dev.azure.com/CFI-AzureDevOps/K12/_git/k12-query-builder/pullrequest/6024

## A3. Azure access and tooling used from the cloud session

- az CLI in a venv: `/root/azcli-venv/bin/az` (cloud session only). Commercial login as Terry.Fraser@randstaddigital.us, subscription **K12 Azure** `cf6841bb-b70c-4d55-a489-2d53855e78b5`. Gov never logged in from the session.
- ADO REST auth: Entra token via `az account get-access-token --resource 499b84ac-1321-427f-aa17-267ca6975798`. Reads used: Git items/refs/commits/pushes/pullrequests, build definitions/builds/timeline, distributedtask pools/agents. ADO PR descriptions cap at 4,000 chars. ADO returned transient 503s once (2026-09-09).
- Azure Resource Graph is the evidence source (`securityresources` assessments; `desc` is reserved, use `descr`; paginate with `--first 1000 --skip-token`, order deterministically).
- ACR data plane (`az acr repository ...`) is blocked from outside the VNet (403); registries are private.

## A4. What was delivered (chronological)

1. Devcontainer verified; devcontainer-platform `extends` bug fixed by inlining base config; `.github-private` init-repos list extended.
2. Remediation queries run against the commercial tenant (k12-cms excluded); results and per-gap confirmations in `k12-Arch/security/azure-remediation/evidence/` (`results/20260907-181549-AzureCloud/`, `results/confirm/`, `04-defender-findings-commercial.md`, `05-plan-confirms-commercial.md`, `01-azure-ado-research-notes.md`).
3. Plans refined with live data: `plans/07-Azure-Security-Remediation-Plan.md` (v0.2, gap register G1–S3 + D4/N7/L4/S3/E1, Wave-0 re-cut, §8 answers), `plans/05-DR-Design-Brief-PI8.md`, `plans/06-CICD-Research-Checklist.md`; `defender-package/` (Defender-Remediation-Plan.md, diagrams 01–04, deck, validated reference Terraform: `terraform/platform-guardrails/{policy,defender}`, `terraform/workload-baseline`, azurerm ~>4.60).
4. K12-8804 (querybuilder image vulns): evidence pack `evidence/k12-8804/` (README, ADO-PR-draft-and-jira-comment.md, standup-brief.md, k12-8804-fix.patch, complete 1,012-row Defender export). Root cause: images built in July from floating `aspnet:10.0`; per image gateway 34 pkgs/15 High, api 33/15, dashboard 33/14; runtime 9 CVEs; NuGet `Microsoft.AspNetCore.OpenApi` 10.0.0 and OpenTelemetry 1.15.0.
5. ADO PR 6024 opened (draft, attribution-free). Discovery: the repo has **three image build paths** (docker-push.yml inline Dockerfile `FROM aspnet:10.0` = what the pipeline pushes; repo Dockerfiles used only by the `az acr build` fallback in app-deploy.yml; `aspire deploy` SDK publish). Fix applied in all three: `aspnet:10.0-noble-chiseled-extra` (+ `ContainerFamily` in Gateway/Api/Dashboard csproj), OpenApi 10.0.11, OpenTelemetry 1.18.0. Chiseled keeps glibc/openssl/ICU/tzdata, so rebuilds remain necessary.
6. Copilot review findings on #148 (7) verified and fixed; threads resolved; PR merged.

## A5. Facts to carry forward

- Live container apps (RG development): `k12-querybuilder-gateway:development-844bf4c` (rev patched0707, external ingress), `k12-querybuilder-api:auditdemo-20260714`, `k12-dashboard:auditdemo-20260714`, plus postgres, cubestore, trino, cube-api, metabase, sqlserver, ops-* copies, and `k12-development-ado-runner` (shown with **no image**).
- App pipeline `k12-query-builder.app` (definition 68, `azure-pipelines.app.yml`): last ran 2026-05-11, all stages green; no PR-validation policy; `docker` stage runs on any non-PR build and pushes to `developmentk12acr`; deploy stages gated to development/main. `deploy-aca.yml` is unreferenced and names `k12-gateway`/`k12-query-api` while live apps are `k12-querybuilder-*`.
- Self-hosted pool `k12-development-pool` (pool 11): 3 agents online (agent v5.278.0, last active 2026-08-31). CI image `developmentk12acr.azurecr.io/k12-ci-sdk:v1` via service connection `k12-acr-registry`. Other definitions: 81 `k12-query-builder` (azure-pipelines.yml, no runs), 83 cube-models, 104 image-relay (`tools/image-relay`, ran 2026-07-07), 106 repo-hygiene.
- Runner image `k12-ado-runner` (K12-8497): 80 vulnerable packages / 44 High / 121 CVEs; 9 distinct digests across developmentk12acr (9), testingk12acr (4), stagingk12acr (2); two Ubuntu generations (24.04 and 22.04). Dockerfile is not in the GitHub mirrors (Dockerfile.ci = CI SDK image; Dockerfile.agent = LangGraph agent); it lives in ADO `k12-infra` under `terraform/modules/core/runner/`.
- ADO `k12-infra@main` (2026-09-13): `terraform/environments/{development,testing,staging}` are commercial (sub `cf6841bb…`), `preprod` and `prod` are Gov (sub `f2a2966b…`, tenant `0e166b06…`, `usgovvirginia`, state `k12infra/tfstate/<env>.tfstate`); preprod applied in July, prod greenfield in code (F21 in Part B questions that). Open PRs: 6078 (Angelo, preprod), 5574 (Terry, runner image contract), 5398 (Abhilash, refund exchange RBAC). Pools: `k12-development-pool` (11), `k12-testing-pool` (12), `k12-staging-pool` (13); no Gov pool. Gov WIF connections: `k12-production`, `k12-dm-gov`.
- Azure Government availability (researched 2026-09-05/07): Managed DevOps Pools **no**; Defender for DevOps **no**; Static Web Apps **no**; Azure Managed Redis **no**; APIM v2 tiers **no US Gov regions**; Container Apps **yes** (FedRAMP High scope; per-region check pending); Front Door Std/Premium, App Gateway WAF v2, SignalR, SB Premium, Functions Premium, Logic Apps Standard, Entra Workload Identities **yes**. PAT-free agent registration (`--auth SP`) and WIF service connections are GA.
- Commercial current state (2026-09-07): secure score 12.9/27; 245 unhealthy recs (65 High); 12 Front Door Standard profiles with **no WAF**; 1 NSG in the estate; 38 private endpoints (App Config, Redis missing); SQL Entra-only off ×4, auditing off; storage shared key on 10/12; 31 Log Analytics workspaces at 30 d, public ingestion; Defender Containers + Resource Manager off; Key Vault purge protection missing ×2; 53 guests with write; no guardrail initiative.

## A6. Parked items (need Terry's call)

- Jira K12-8804 comment: drafted in `evidence/k12-8804/ADO-PR-draft-and-jira-comment.md`, **not posted**.
- Pipeline run for PR 6024: **Terry said do not queue** (2026-09-12).
- PR 6024: still draft, no reviewers.
- Runner image rebuild (K12-8497): not started. Dockerfile located: ADO `k12-infra@main` `terraform/modules/core/runner/{Dockerfile,start.sh,smoke-test.sh,README.md}` (release contract in `terraform/docs/ADO_RUNNER_STABILIZATION.md`, open PR 5574). Folded into Part B (B2.3 runner image).
- Gov tenant remediation queries: not run (`evidence/run-remediation-queries.sh --cloud AzureUSGovernment`).
- Cloud-session housekeeping: done 2026-09-13 — the #148 check-in routines were one-shot and are disabled; the #148 subscription ended automatically on merge.

## A7. Optional: pulling this session's transcript locally

Not required for the handoff. If wanted: `claude --teleport session_01QETvmMy74iBEHm3QVfqpBt` from a local checkout of `tfxdevelopment/k12-infra` (the session's first source repo), clean working tree, same claude.ai account, Claude Code ≥ 2.1.223. It fetches the session branch, which does **not** yet exist on the k12-infra remote, so push it first or use "Open in > Terminal" from the session menu. The terminal copy is independent of the cloud session.

## A8. What happened after the plan was approved (2026-09-13)

- Plan file split into this handoff and plan 08; discovery script and PR-6078 review-note drafts written under `security/azure-remediation/evidence/gov-preprod/`.
- ADO `k12-infra` cloned read-only at `feature/preprodUpdates@3e0a960e` in the cloud session; branch preparation (plan 08 B2.1–B2.3) starts there. Nothing pushed to ADO without approval.
- Jira stories under K12-7534 (plan 08 B3 step 0) are drafted, not created, until Terry approves the titles.
