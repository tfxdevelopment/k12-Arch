# Draft review comments for ADO PR 6078 (`feature/preprodUpdates` → `main`) — NOT POSTED

Status: drafts for Terry's approval (plan 08, B3 step 5). Terry chose "Both": the fixes land in our stacked branch, and these comments go to Angelo so PR 6078 itself gets corrected. Post them without any AI attribution. Each item names the file in Angelo's branch and the suggested change; F-numbers refer to plan 08 §B1.

| # | File (branch `feature/preprodUpdates`) | Draft comment |
|---|---|---|
| F4 | `terraform/environments/preprod/main.tf`, `module "enrollment-api"` | `sql_azuread_admin_login` / `sql_azuread_admin_object_id` were dropped. The module defaults are the commercial group `CFI-AzureDevOps - K12 - Contributors` (`50a9f81f…`), which does not exist in the Gov tenant, so the SQL server's Entra admin would be set to an unknown principal. Suggest restoring `sql_azuread_admin_login = "k12-devsecops-randstad"` and `sql_azuread_admin_object_id = "8990340b-2506-4b8f-9c4e-a34ff8d8077d"` (same values main has today). |
| F5 | same block | `create_adf = true` was dropped; the module default is `false`, so the plan will destroy `preprod-api-enrollment-adf` and its private endpoint. If preprod should keep the Data Factory (it was created in July), please restore `create_adf = true`. |
| F6 | same block | `k12_contributors_kv_rw_principal_id_prod` was dropped. For preprod the module uses that variable as the principal of the Key Vault Secrets Officer role assignment (`azurerm_role_assignment.k12_contributors_kv_rw`), so an empty value fails the apply and removes the devsecops group's secret access. Suggest restoring `"8990340b-2506-4b8f-9c4e-a34ff8d8077d"`. |
| F7 | same block | `website_contentshare = "preprod-api-enrollment"` was dropped; the API runs with `WEBSITE_CONTENTOVERVNET=1`, which needs a valid share name. Suggest restoring it. |
| F2 | `terraform/environments/preprod/locals.tf`, `sql_connection_string` | Still uses the commercial suffix `preprod-api-enrollment.database.windows.net`. In Gov the server is `preprod-api-enrollment.database.usgovcloudapi.net`. |
| F3 | `locals.tf`, `blob_storage_connection_string` | Still the greenfield placeholder. Since `data.tf` now reads `storage-account-key`, this can become `DefaultEndpointsProtocol=https;AccountName=preprodapienrollment;AccountKey=${data.azurerm_key_vault_secret.storage_account_key.value};EndpointSuffix=core.usgovcloudapi.net` (note the Gov endpoint suffix; staging's `core.windows.net` does not apply). |
| F8 | `locals.tf` agent block + `modules/core/devops_agents.tf` | The agent settings were copied from staging: pool `k12-staging-pool` (preprod jobs would land in the commercial staging pool) and image `stagingk12acr.azurecr.io/k12-ado-runner@sha256:8b66…` (a commercial registry with public access disabled; Gov ACI cannot pull it, and `mi-k12-ops-acr-pull` is now looked up in RG `preprod`, where it may not exist). Proposal on our side: keep `enable_devops_agents = false` for preprod and add the Gov agent pool as Container Apps Jobs (plan 08 §B2.3), with the runner image in `preprodk12acr.azurecr.us`. If you prefer to keep ACI here, the pool should be a new `k12-preprod-pool` and the image must come from a Gov registry. |
| F9 | `preprod/main.tf`, `module "web-apis"` | `apis` names are staging's (`k12-stage-*`) — preprod should use `k12-preprod-*`. `entra_valid_audiences` lists the six commercial app registration IDs; the preprod APIs must accept the Gov tenant app registrations from `k12-entra-infra/Preprod-*` (K12-9397/K12-9409 still open), so this list needs the Gov client IDs or should stay empty with a TODO. `modules/web-apis/main.tf` Redis: `location = var.location` is simpler than the environment-name conditional. |
| F11 | `preprod/main.tf`, `module "enrollment-api"` | `signalr_public_network_access_enabled = true` re-opens the SignalR public endpoint although the module already creates a SignalR private endpoint. Suggest `false` once private DNS resolution is validated in Gov (see the DNS zone note below). |
| F1 (informational) | `modules/core/dns.tf`, `modules/api-enrollment/network.tf` | Not introduced by this PR, but it affects preprod: the private DNS zones are created with the commercial names (`privatelink.database.windows.net`, `privatelink.azurecr.io`, …). In Gov the private-link zones are `privatelink.database.usgovcloudapi.net`, `privatelink.vaultcore.usgovcloudapi.net`, `privatelink.*.core.usgovcloudapi.net`, `privatelink.azurecr.us`, `privatelink.servicebus.usgovcloudapi.net`, `privatelink.azurewebsites.us`. Our stacked branch makes the zone map cloud-aware; flagging it here so the apply order is understood. |
| F13 (informational) | `modules/core/app_regs.tf` | `azurerm_key_vault_access_policy.devops_sp_policy` targets a vault that has RBAC authorization enabled; access policies are rejected there. Our branch gates it off for RBAC vaults. |

Suggested PR-level comment (summary): "Thanks for getting preprod moving. Before this merges, a few values carried over from staging will not work in the Gov tenant (SQL Entra admin, KV role principal, agent pool/image, web-apis audiences and names, connection-string suffixes). We are stacking a branch on `feature/preprodUpdates` with the Gov-correct values and the hardening items agreed with Terry; details per line above."
